apiVersion: v1
kind: Service
metadata:
  name: job-scheduler-service
  namespace: scheduler-app
  labels:
    app: job-scheduler
spec:
  selector:
    app: job-scheduler
  ports:
  - name: http
    port: 8081
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
